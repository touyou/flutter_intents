/// Base class for WidgetKit widget configuration specifications.
///
/// See `WidgetConfigurationSpec` for what gets generated and why the output
/// belongs to its own Xcode target.
abstract class WidgetConfigurationSpecBase {
  const WidgetConfigurationSpecBase();
}
